# 작업 목록 - 2026년 2월 2일

**작성일**: 2026-02-02  
**작업 담당**: 개발팀  
**우선순위**: 🔴 높음 (사용자 경험에 직접적인 영향)

---

## 📋 작업 개요

### 문제
**이벤트 대시보드의 추천인 Top12 숫자가 업데이트되지 않고 일부 직원의 카운트가 보이지 않는 현상**

### 원인
- Supabase JavaScript 클라이언트의 1000행 제한
- 전체 8,944명 중 1,000명만 조회되고 7,939명(88.8%)이 누락됨
- `AdminEventDashboard.jsx`의 `referrerStatsQuery`에 페이지네이션 미적용

### 영향
- 최근에 추천을 시작한 직원의 카운트가 0으로 표시
- Top 12 순위가 부정확하게 표시됨
- 숫자가 실시간으로 업데이트되지 않는 것처럼 보임

---

## ✅ TODO: 수정 작업

### 🎯 작업 1: AdminEventDashboard.jsx 페이지네이션 추가

**파일**: `src/components/Admin/AdminEventDashboard.jsx`  
**함수**: `loadData()` (Line 237-492)  
**수정 위치**: Line 342-371

#### 현재 코드 (문제):
```javascript
// Line 342-371
let referrerStatsQuery = supabase
  .from('event_participants')
  .select('referrer_name, referrer_code')
  .not('referrer_code', 'is', null)

// ... 권한별 필터링 로직 ...

// 이벤트 필터 적용
if (selectedEvent) {
  referrerStatsQuery = referrerStatsQuery.eq('event_name', selectedEvent)
}

const { data: referrerStats, error: referrerError } = await referrerStatsQuery
// ⚠️ 문제: 페이지네이션 없이 한 번에 조회 → 1000개만 반환
```

#### 수정할 코드 (해결책):
```javascript
// Line 342-369는 그대로 유지
let referrerStatsQuery = supabase
  .from('event_participants')
  .select('referrer_name, referrer_code')
  .not('referrer_code', 'is', null)

// 권한별 필터링
if (determinedViewMode === 'user' && user?.referral_code) {
  referrerStatsQuery = referrerStatsQuery.eq('referrer_code', user.referral_code)
} else if (determinedViewMode === 'admin' && user?.branch) {
  const { data: branchUsers } = await supabase
    .from('users')
    .select('referral_code')
    .eq('branch', user.branch)
    .not('referral_code', 'is', null)

  const branchReferralCodes = branchUsers?.map(u => u.referral_code) || []

  if (branchReferralCodes.length > 0) {
    referrerStatsQuery = referrerStatsQuery.in('referrer_code', branchReferralCodes)
  } else {
    referrerStatsQuery = referrerStatsQuery.eq('referrer_code', 'NONE')
  }
}

// 이벤트 필터 적용
if (selectedEvent) {
  referrerStatsQuery = referrerStatsQuery.eq('event_name', selectedEvent)
}

// ✅ 수정: Line 371을 아래 페이지네이션 코드로 교체
let allReferrerData = []
let from = 0
const pageSize = 1000

while (true) {
  const { data: pageData, error: pageError } = await referrerStatsQuery
    .range(from, from + pageSize - 1)
  
  if (pageError) throw pageError
  if (!pageData || pageData.length === 0) break
  
  allReferrerData = allReferrerData.concat(pageData)
  
  if (pageData.length < pageSize) break
  from += pageSize
}

const referrerStats = allReferrerData
const referrerError = null

// Line 373-376은 그대로 유지
if (referrerError) {
  console.error('▶ 추천인별 통계 에러:', referrerError)
}

// 이후 Line 378-413은 그대로 사용
```

#### 참고 코드:
- **같은 파일의 Line 427-441**에 동일한 패턴이 이미 구현되어 있음
- `branchStatsQuery`가 페이지네이션을 올바르게 사용하는 예시임

---

## 🔍 작업 2: 테스트 및 검증

### 테스트 항목:
- [ ] 전체 참가자 수가 올바르게 표시되는지 확인
- [ ] Top 12 추천인 순위가 진단 결과와 일치하는지 확인
- [ ] 모든 직원의 카운트가 정확하게 표시되는지 확인
- [ ] 새로운 참가자 등록 시 실시간 업데이트가 동작하는지 확인
- [ ] 이벤트 필터 적용 시 정상 동작하는지 확인
- [ ] 지점 필터 적용 시 정상 동작하는지 확인

### 예상 결과 (수정 후):
```
🥇 1위 | LAS1174 | 712명
🥈 2위 | LAS1095 | 537명
🥉 3위 | LAS3002 | 402명
 4위  | LAS1037 | 367명 (김정선)
 5위  | LAS5001 | 339명
 6위  | LAS1006 | 261명
 7위  | LAS1133 | 213명
 8위  | LAS1107 | 190명
 9위  | LAS1164 | 180명
10위  | LAS5056 | 176명
11위  | LAS1192 | 173명
12위  | LAS1110 | 173명
```

---

## 📚 참고 자료

### 진단 보고서:
- `sql/TOP12_문제_진단보고서.md` - 상세 진단 결과
- `sql/diagnose_top12_issue.sql` - SQL 진단 쿼리
- `sql/run_diagnosis.js` - 자동 진단 스크립트

### 진단 결과 요약:
- 전체 참가자: 8,944명
- 추천인 코드 있는 참가자: 8,939명
- 기존 코드로 조회되는 데이터: 1,000명
- **누락된 데이터: 7,939명 (88.8%)**

### 데이터베이스 상태:
- ✅ 데이터베이스는 정상 (트리거, 함수 수정 불필요)
- ✅ 모든 데이터가 올바르게 저장되어 있음
- ✅ users 테이블과의 매칭 정상
- ✅ 데이터 무결성 문제 없음

---

## ⚠️ 주의사항

### 수정하지 말아야 할 것:
- ❌ 데이터베이스 트리거
- ❌ 데이터베이스 함수
- ❌ 테이블 스키마
- ❌ RPC 함수
- ❌ SQL 쿼리

### 수정해야 할 것:
- ✅ **오직 `AdminEventDashboard.jsx` 파일의 JavaScript 코드만**

### 백업:
- 수정 전 `AdminEventDashboard.jsx` 파일을 백업해두는 것을 권장
- Git 커밋 전 테스트 완료 필수

---

## 🚀 예상 작업 시간

- 코드 수정: 10분
- 테스트: 20분
- 총 소요 시간: **약 30분**

---

## 📝 체크리스트

- [ ] `AdminEventDashboard.jsx` 파일 백업
- [ ] Line 371 부분을 페이지네이션 코드로 교체
- [ ] 로컬 환경에서 빌드 확인
- [ ] 개발 서버에서 동작 테스트
- [ ] Top 12 순위가 올바르게 표시되는지 확인
- [ ] 모든 직원의 카운트가 정확한지 확인
- [ ] 새 참가자 등록 시 실시간 업데이트 확인
- [ ] 필터 기능 정상 동작 확인
- [ ] Git 커밋 및 푸시
- [ ] 프로덕션 배포

---

**작업 완료 예정일**: 2026-02-02  
**작업자**: _______________  
**검토자**: _______________
